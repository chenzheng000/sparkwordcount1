apiVersion: v1
kind: Service
metadata:
  labels:
    app: kubesphere
    component: ks-dev-test
  name: ks-dev-test
  namespace: kubesphere-sample-dev
spec:
  ports:
    - name: http
      port: 8081
      protocol: TCP
      targetPort: 8081
      nodePort: 30862
  selector:
    app: kubesphere
    component: ks-dev-test
    tier: backend
  sessionAffinity: None
  type: NodePort
  
  
  需要做的是1.修改yaml文件，2.替换流水线中文件名称（已经部署了一个）不然就会把原来的替代掉，然后运行